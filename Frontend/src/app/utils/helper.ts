import { BloodGroup, Gender, Specialisation } from './enums';

// MODEL HELPERS
export enum Flag {
  CRITICAL = 'CRITICAL',
  ABNORMAL = 'ABNORMAL',
  NORMAL = 'NORMAL',
}

export enum FollowupStatus {
  Pending = 'Pending',
  Missed = 'Missed',
  Acheived = 'Acheived',
}
